INCLUDES = $(all_includes)
METASOURCES = AUTO
SUBDIRS = screens lesson_data fonts images source_data
desktopdir = $(datadir)/applications
desktop_DATA = hypertype.desktop
EXTRA_DIST = $(desktop_DATA)
